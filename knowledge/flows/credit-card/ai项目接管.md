请只读取和分析【成熟仓库】代码，不修改任何代码，也不要处理当前 AI 项目。

目标：从成熟仓库中提取可复用的项目框架、页面组织方式、公共组件接入方式、校验提交流程、请求异常处理流程和文件职责分层，作为后续 AI 项目向成熟仓库对齐的事实基线。

注意：不要根据通用最佳实践编造规范。所有结论必须来自成熟仓库真实代码，并附真实文件路径。

请按以下顺序分析：

## 1. 项目入口与运行方式

读取并总结：

 package.json；
 构建配置，例如 webpack  vite  babel；
 src 入口；
 App  root  layout；
 环境配置；
 路由入口。

输出：

 项目如何启动；
 页面如何进入；
 全局能力在哪里初始化；
 哪些构建和依赖信息只是兼容性参考，不允许直接复制到其他项目。

## 2. 目录结构与职责

输出成熟仓库主要目录结构，并说明每个目录真实职责。

需要区分：

 页面业务目录；
 公共组件目录；
 请求API目录；
 校验相关目录；
 配置目录；
 工具函数目录；
 状态管理目录；
 环境mockstub目录；
 历史遗留或不建议复用目录。

## 3. 页面类型样本

请识别成熟仓库中的主要页面类型，并每类选择 1～3 个真实案例。

至少覆盖：

 查询列表页；
 详情页；
 新增页；
 编辑页；
 带 Submit 的表单页；
 带字段校验的页面；
 带弹窗确认的页面；
 带初始化接口的页面。

每个案例输出：

 页面路径；
 页面入口文件；
 页面文件组成；
 页面职责；
 是否属于稳定模式；
 是否存在特殊写法或历史遗留风险。

## 4. 页面标准文件组成

请总结成熟仓库中一个页面通常由哪些文件组成。

重点确认：

 页面入口文件；
 配置文件；
 常量文件；
 校验文件；
 请求参数文件；
 API 文件；
 工具函数文件；
 组件拆分文件；
 样式文件；
 mock  stub 文件。

请说明每类文件的职责，不能只列文件名。

## 5. 公共组件接入方式

请分析成熟仓库中以下组件场景的真实用法：

 表单；
 输入框；
 下拉框；
 日期；
 表格；
 分页；
 弹窗；
 按钮；
 loading；
 toast  message；
 错误提示；
 空状态。

每个场景输出：

 使用的组件名称；
 引入路径；
 页面中如何传参；
 如何绑定字段；
 如何展示错误；
 是否依赖特定版本组件库；
 参考文件路径；
 是否可以作为 AI 项目对齐标准。

## 6. 校验与 Submit 完整流程

这是重点。

请至少选择 3 个带 Submit 和校验的页面，提取完整链路：

 用户点击 Submit；
 触发哪个页面方法；
 校验规则从哪里加载；
 校验文件放在哪里；
 校验如何执行；
 字段错误如何回填到组件；
 校验失败如何阻断后续流程；
 校验通过后如何进入参数组装；
 请求参数在哪里组装；
 API 在哪里调用；
 loading 如何控制；
 接口成功如何处理；
 接口失败如何处理；
 成功后是刷新、跳转、返回还是停留当前页。

必须输出完整调用链，不允许只写“调用 submit 方法”。

## 7. API、请求、异常处理流程

请分析成熟仓库中的请求体系：

 API 文件位置；
 请求函数定义方式；
 请求参数处理方式；
 接口返回数据处理方式；
 错误码处理方式；
 loading 处理方式；
 toast  message 触发方式；
 dev  sit  mock  stub 环境切换方式；
 页面是否允许直接请求接口。

请区分：

 稳定通用模式；
 页面专用模式；
 不建议复用的历史写法。

## 8. 状态、常量、配置、工具函数

请总结成熟仓库中以下内容的真实放置方式：

 页面状态；
 表单状态；
 查询条件；
 表格数据；
 枚举；
 展示文案；
 常量；
 字段映射；
 工具函数；
 页面专属函数；
 跨页面公共函数。

重点说明：

 哪些应该放页面内；
 哪些应该放配置文件；
 哪些应该放工具函数；
 哪些可以抽公共；
 哪些不应该随意抽公共。

## 9. 兼容性与可迁移性判断

请根据成熟仓库代码，输出哪些内容适合后续 AI 项目对齐。

分类为：

### 可直接对齐

例如目录组织、页面文件职责、校验文件位置、Submit 调用链等。

### 需适配后对齐

例如组件库 API、请求封装、路由跳转、状态管理等，需要确认当前 AI 项目依赖后才能落地。

### 不应直接复制

例如 webpack 配置、Node 版本、Babel 插件、lock 文件、成熟库特定业务公共模块、历史遗留代码。

### 待确认

证据不足，不能判断为通用标准的内容。

## 10. 输出文档

请生成以下 Markdown 文档：

 mature-baseline00-compatibility.md
 mature-baseline01-repository-map.md
 mature-baseline02-routing-entry.md
 mature-baseline03-page-type-catalog.md
 mature-baseline04-page-structure.md
 mature-baseline05-component-patterns.md
 mature-baseline06-validation-submit.md
 mature-baseline07-api-request-flow.md
 mature-baseline08-state-config-utils.md
 mature-baseline09-adaptation-rules.md
 mature-baseline10-reference-index.md

要求：

 不修改任何业务代码；
 不新增新架构；
 不把单个页面写法直接认定为全局规范；
 每条结论必须附真实文件路径；
 不确定的内容必须标记为“待确认”；
 必须区分“稳定通用模式”“页面类型专用模式”“特殊实现”“历史遗留写法”；
 重点提取完整流程，不要只做目录说明。


