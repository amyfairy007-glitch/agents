请对成熟模板 Journey 的 Step 1 实现，以及当前“清除业务代码后”的新 Journey Step 1，做一次只读联合审计。

## 一、任务背景

当前流程是：

1. 基于成熟模板 Journey 创建新 Journey。
2. 清除成熟模板中的业务代码。
3. 为新 Journey 填充 Step 1 业务脚本。

但当前存在风险：

此前“清除业务代码”的实现，可能没有先完整分析成熟模板 Step 1 的真实调用链、公共逻辑与业务逻辑边界。

因此，目前不能直接假设清理结果可靠。

本次任务的目标不是继续填充 Step 1，而是先确认：

* 成熟模板 Step 1 实际如何运行；
* 哪些代码是真正公共的；
* 哪些内容属于成熟 Journey 业务；
* 当前清理过程实际删除了什么；
* 当前清理结果是否正确、完整、可运行；
* 当前清理规则是否具备继续复用的可靠性。

本次只分析，不修改任何文件。

## 二、核心原则

以下内容都只是人工理解，不是既定事实：

* 登录可能是公共实现；
* 导航到 Journey 可能是公共实现；
* fields 采集可能是公共实现；
* Journey 差异可能集中在进入页面后的操作；
* 当前业务清理可能已经完成。

必须以真实代码、配置、调用链和产物为准。

不得为了符合当前设计而忽略反例。

## 三、分析对象

请识别并读取以下内容：

1. 成熟模板 Journey 的 Step 1 正式入口。
2. 成熟模板 Step 1 的完整调用链。
3. 成熟模板 Step 1 直接依赖的配置、数据、工具函数和辅助文件。
4. 当前新 Journey 清理后的 Step 1。
5. 当前新 Journey 清理后的相关配置、数据和辅助文件。
6. 执行业务清理的脚本、Skill、Prompt、规则或实现代码。
7. 如存在清理前后的记录、日志、报告或 diff，一并纳入分析。

只读取与 Step 1 和业务清理直接相关的文件，不全量扫描仓库。

## 四、先还原成熟模板 Step 1 的真实实现

请从正式执行入口开始，确认：

* 启动命令；
* 入口文件；
* 主函数；
* 浏览器初始化；
* 登录；
* 公共环境准备；
* 菜单导航；
* Journey 定位；
* Journey 页面内操作；
* 测试数据；
* 等待与成功判断；
* fields 采集；
* 截图；
* 输出；
* 错误处理；
* 清理与关闭。

不要强行套用以上阶段。

不存在的阶段应明确标注，实际存在的额外阶段也要补充。

输出真实调用链：

```text
启动命令
→ 入口文件 / 函数
→ 公共逻辑
→ Journey 专属逻辑
→ 采集逻辑
→ 输出产物
```

每一步必须标明文件路径和函数。

## 五、识别公共代码与成熟 Journey 业务代码

请对 Step 1 相关代码逐项分类：

* 确定属于公共实现；
* 确定属于成熟 Journey 专属业务；
* 公共与业务混合；
* 无法确认。

重点识别：

* Journey 名称；
* 菜单名称；
* URL、路由；
* 页面标题；
* selector；
* 输入值；
* 测试数据；
* 点击、输入、选择操作；
* 表格、Tab、弹窗、iframe；
* 等待条件；
* 成功判断；
* 请求等待；
* 截图名称；
* 输出标识；
* 配置键；
* 业务辅助函数；
* 硬编码字段；
* 与成熟 Journey 绑定的注释或说明。

输出表格：

| 文件 | 函数/代码块 | 内容 | 分类 | 判断依据 |
| -- | ------ | -- | -- | ---- |

## 六、分析当前“清除业务代码”的实现

请找到当前负责清除业务内容的实现，并说明：

1. 清理入口在哪里。
2. 清理是由脚本、Skill、Prompt 还是人工规则执行。
3. 清理依据是什么：

   * 关键词；
   * 固定代码块；
   * AST；
   * 文件模板；
   * 配置清单；
   * diff；
   * AI 判断；
   * 其他方式。
4. 清理操作会读取哪些文件。
5. 会修改哪些文件。
6. 会删除、替换或保留哪些内容。
7. 是否理解 Step 1 调用链。
8. 是否理解公共逻辑和 Journey 业务边界。
9. 是否检查清理后的可运行性。
10. 是否检查模板业务残留。

如果清理实现没有读取成熟模板真实实现，请明确指出。

## 七、对比成熟模板与清理后新 Journey

请逐文件对比：

* 成熟模板 Step 1；
* 清理后新 Journey Step 1；
* 对应配置；
* 对应数据文件；
* 对应辅助函数。

输出：

### 1. 正确清除的内容

这些内容确实属于成熟 Journey 业务，且清除方式合理。

### 2. 遗漏的业务残留

包括：

* 名称；
* selector；
* 路径；
* 测试数据；
* 页面文本；
* 等待条件；
* 配置；
* 辅助函数；
* 输出标识；
* 其他隐藏残留。

### 3. 可能误删的公共代码

说明：

* 被删除内容；
* 原始职责；
* 为什么可能属于公共实现；
* 对调用链的影响。

### 4. 被破坏的结构

检查是否存在：

* 函数体不完整；
* 参数不完整；
* 调用丢失；
* 配置缺失；
* import 无效；
* 变量未定义；
* 语法虽然成立但运行逻辑中断；
* 采集逻辑无法再被触发。

### 5. 表面清理但实际未解耦的内容

例如：

* 主脚本已清空，但工具函数仍写死模板 Journey；
* 配置改名了，但数据仍是模板数据；
* selector 被删了，但页面判断仍依赖模板标题；
* 页面动作清空了，但等待条件仍绑定模板页面。

## 八、评估清理结果是否可靠

请给出明确结论：

* 可靠；
* 基本可靠但存在少量问题；
* 部分可靠；
* 不可靠；
* 证据不足。

并分别评估：

| 项目                    | 结论 | 证据 |
| --------------------- | -- | -- |
| 公共逻辑是否保留              |    |    |
| 成熟业务是否清除完整            |    |    |
| 新 Journey 脚本结构是否完整    |    |    |
| Step 1 是否仍可运行         |    |    |
| 是否适合继续做业务填充           |    |    |
| 当前清理规则能否复用于其他 Journey |    |    |

## 九、给出正确的清理边界

基于成熟模板真实实现，列出：

### 必须保留

* 文件；
* 函数；
* 代码块；
* 配置；
* 数据结构；
* 公共调用关系。

### 必须清除

* 成熟 Journey 专属操作；
* 成熟 Journey 数据；
* 成熟 Journey selector；
* 成熟 Journey 页面判断；
* 成熟 Journey 输出标识；
* 其他业务绑定内容。

### 不应直接删除、需要改为占位或保留结构

例如：

* 函数壳；
* 回调接口；
* 配置键；
* Journey hook；
* 页面操作函数；
* 测试数据入口；
* 等待函数调用位置。

## 十、评估当前清理方式

请回答：

1. 当前清理方式是否只是关键词级清理。
2. 是否依赖对模板实现的理解。
3. 是否会误删公共逻辑。
4. 是否会漏删跨文件业务内容。
5. 是否能稳定用于不同 Journey。
6. 是否应继续使用当前清理方式。
7. 后续应采用：

   * 精确文件/函数范围清理；
   * 明确占位块；
   * 配置化替换；
   * 模板生成；
   * 其他基于现有实现的方式。

不要设计全新架构，只给出最小修正建议。

## 十一、输出格式

### 1. 结论摘要

说明：

* 成熟模板 Step 1 实际结构；
* 当前清理实现是否理解该结构；
* 当前清理结果是否可信；
* 是否可以直接进入 Step 1 填充。

### 2. 成熟模板 Step 1 调用链

列出文件、函数和产物。

### 3. 公共代码与业务代码边界

| 文件 | 函数/代码块 | 类型 | 是否应保留 |
| -- | ------ | -- | ----- |

### 4. 当前清理实现

* 入口；
* 原理；
* 修改范围；
* 判断依据；
* 局限。

### 5. 清理前后对比结果

* 正确清除；
* 遗漏残留；
* 误删内容；
* 结构破坏；
* 隐藏风险。

### 6. 可靠性判断

给出明确等级和证据。

### 7. 正确清理清单

| 文件 | 位置 | 应保留/清除/占位 | 原因 |
| -- | -- | --------- | -- |

### 8. 后续建议

说明：

* 是否应先修复清理逻辑；
* 是否需要重新生成当前新 Journey；
* 当前新 Journey 是否可继续使用；
* 修复后才能进入的下一步是什么。

### 9. 未确认事项

列出必须通过运行或人工确认的问题。

## 十二、禁止事项

* 不修改文件。
* 不直接修复清理脚本。
* 不继续填充 Step 1。
* 不分析 Step 2～Step 9。
* 不把当前清理结果当作正确结果。
* 不把人工理解当成事实。
* 不只检查文件名和关键词。
* 不只检查主脚本，必须检查直接依赖。
* 不提出脱离现有实现的新架构。
* 不在没有证据时声称清理成功。

最终必须明确回答：

> 当前清除业务代码的实现是否可靠，以及在继续 Step 1 脚本填充之前，是否必须先修正或重新执行清理。
