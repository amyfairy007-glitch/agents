请对当前新 Journey 的 Step 1～Step 9“成熟模板业务清除结果”进行一次只读验收。

## 一、任务背景

当前流程已经完成：

1. 基于成熟模板 Journey 创建新 Journey；
2. 对新 Journey 的 Step 1～Step 9 执行业务清除；
3. 下一阶段准备填充目标 Journey 自己的业务内容。

但当前业务清除实现没有充分基于 Step 1～Step 9 各自的真实调用边界进行清理，因此现有清除结果不能直接判定可靠。

本次任务只做验收和审计，不修改任何文件，不填充目标 Journey 业务。

## 二、任务目标

验证以下内容：

1. 成熟模板 Journey 在 Step 1～Step 9 中的业务内容是否已经清除；
2. 每一步的公共结构、调用链和产物关系是否完整保留；
3. 是否存在成熟模板业务残留；
4. 是否误删了公共逻辑；
5. 是否破坏了步骤之间的输入输出关系；
6. 当前新 Journey 是否可以作为后续九步业务填充的可靠底板。

## 三、重要原则

以下均不能直接当作事实：

* 每一步的公共代码范围；
* 每一步的 Journey 专属业务范围；
* 当前清除结果已经正确；
* Step 1～Step 9 可以使用同一种清除规则；
* 某些相似代码一定属于公共代码；
* 某些被删除代码一定属于业务代码。

必须以成熟模板真实代码、配置、调用链、步骤产物和清理前后 diff 为依据。

## 四、分析范围

请定位并对比：

* 成熟模板 Journey 的 Step 1～Step 9；
* 当前清理后的新 Journey Step 1～Step 9；
* 各步骤直接依赖的配置、数据、selector、helper、脚本和 Schema；
* 步骤之间传递的中间产物；
* 当前负责创建新 Journey 和清除业务内容的实现；
* 清理日志、报告或 diff，如存在。

只读取与九步流程和清除实现直接相关的文件，不全量扫描无关仓库内容。

## 五、先还原九步真实结构

请分别分析 Step 1～Step 9，不要只根据步骤名称猜职责。

对每一步确认：

| 项目         | 需要确认的内容            |
| ---------- | ------------------ |
| 正式入口       | 命令、文件、函数           |
| 输入         | 配置、文件、参数、上一步产物     |
| 核心执行       | 实际调用链              |
| 公共逻辑       | 可复用且不应清除的内容        |
| Journey 业务 | 与成熟模板业务绑定的内容       |
| 输出         | 文件、JSON、截图、报告或其他产物 |
| 下游         | 哪一步使用该产物           |
| 异常处理       | 失败方式和状态记录          |

输出每一步的真实调用链，例如：

```text
Step N 入口
→ 输入读取
→ 公共处理
→ Journey 专属处理
→ 产物生成
→ 输出给 Step N+1
```

以上结构仅为示例，必须以真实实现为准。

## 六、分别建立 Step 1～Step 9 的业务指纹

必须从成熟模板的真实实现中提取，不得只搜索 Journey 名称。

业务指纹可能包括：

* Journey 名称及变体；
* URL、菜单、页面标题；
* selector；
* 页面操作；
* 测试数据；
* 字段名及字段映射；
* API、请求参数、响应字段；
* validation 规则；
* action、场景、状态；
* 表格列、Tab、弹窗、iframe；
* 等待条件和成功判断；
* 截图名称；
* 输出目录或文件名；
* JSON 中的业务标识；
* 业务专属配置键；
* 业务辅助函数；
* 白名单、忽略项、映射规则；
* 模板 Journey 特有注释或说明。

请按步骤分别输出业务指纹，不能把九步混成一份泛化列表。

## 七、检查每一步的业务残留

在当前新 Journey 中逐步搜索成熟模板业务残留。

对每个残留分类为：

* 无残留；
* 可解释残留；
* 不可解释残留；
* 证据不足。

必须记录：

| Step | 业务指纹 | 文件/位置 | 当前内容 | 分类 | 判断依据 |
| ---- | ---- | ----- | ---- | -- | ---- |

只要任意一步存在不可解释的成熟业务残留，就不能判定九步清除全部通过。

## 八、检查是否误删公共逻辑

逐步检查清理前后是否保留：

* 正式入口；
* import 和依赖；
* 函数签名；
* 公共初始化；
* 公共登录、导航或环境准备；
* 通用采集器；
* 通用转换、标准化和比较逻辑；
* 公共输出逻辑；
* Schema；
* 日志和异常处理；
* 资源关闭；
* 步骤状态写入；
* 步骤之间的产物传递。

重点识别：

* 函数被清空后调用链中断；
* 变量或配置被删除后未定义；
* import 仍存在但目标实现被删；
* 公共函数被误判成业务代码；
* 上一步仍生成产物，但下一步读取路径被删；
* 输出格式被破坏；
* try/catch/finally 被破坏；
* 脚本语法成立但语义链路已失效。

## 九、检查步骤间依赖是否完整

验证九步之间的实际依赖关系：

```text
Step 1 输出 → Step 2 或后续使用
Step 2 输出 → Step 3 使用
……
Step 8 输出 → Step 9 使用
```

具体依赖必须以真实代码为准。

检查：

* 文件名和路径是否仍一致；
* JSON 结构是否保留；
* 必需字段是否被误删；
* Journey 业务值是否已清空但结构仍存在；
* 下游是否仍能读取清理后的占位产物或配置；
* 是否存在某一步清理后导致后续步骤无法启动；
* 是否误将公共 Schema 当作业务内容删除。

## 十、检查清理后是否存在明确填充位置

对 Step 1～Step 9 分别判断：

* 是否有明确业务填充函数；
* 是否有明确配置占位；
* 是否保留了函数壳；
* 是否保留必要参数；
* 是否只留下无意义空文件；
* 是否需要同时填代码和配置；
* 是否存在多个分散填充位置；
* 是否能让后续 Skill 精确定位修改范围。

输出：

| Step | 填充文件 | 填充函数/配置 | 当前状态 | 是否可直接填充 |
| ---- | ---- | ------- | ---- | ------- |

## 十一、检查当前清除实现

分析负责清除 Step 1～Step 9 业务内容的真实实现：

1. 清除入口；
2. 读取哪些文件；
3. 修改哪些文件；
4. 是否逐步分析 Step 1～Step 9；
5. 是否使用统一规则强行处理全部步骤；
6. 是否只按关键词删除；
7. 是否基于固定代码块或函数边界；
8. 是否检查跨文件业务内容；
9. 是否检查步骤间依赖；
10. 是否验证清理后的结构；
11. 是否扫描成熟模板残留；
12. 是否可能误删公共能力。

明确说明当前实现是否具备九步边界化清理能力。

## 十二、静态验证

使用仓库已有命令执行：

* Step 1～Step 9 脚本语法检查；
* lint；
* import 和引用检查；
* Schema 校验；
* 现有相关测试；
* 九步流程已有的 dry-run 或预扫描命令。

不得新增测试框架。

记录实际执行命令、结果和失败原因。

## 十三、运行边界验证

如环境允许，验证清理后的九步流程。

由于目标 Journey 业务尚未填充，不要求九步完整成功，但必须确认：

* 各步骤能否进入正式入口；
* 公共初始化是否正常；
* 是否能到达各自业务填充边界；
* 是否以明确、预期的方式停止；
* 是否错误执行成熟模板业务；
* 是否在错误状态继续生成产物；
* 是否因误删公共结构出现异常；
* 上一步未完成时，下游是否会正确阻止执行。

无法运行的项目必须标记为“未验证”，不得写成通过。

## 十四、通过标准

只有以下条件全部满足，才可判定九步业务清除通过：

* Step 1～Step 9 均已识别真实公共边界和业务边界；
* 无不可解释的成熟模板业务残留；
* 九步公共结构均完整保留；
* 步骤入口、函数、参数和 import 完整；
* 步骤之间的输入输出结构完整；
* 业务值已清除，但公共 Schema 未被破坏；
* 每一步都有明确的后续业务填充位置；
* 静态检查通过；
* 运行时能到达预期业务边界，或明确记录未验证；
* 未修改九步流程之外的无关内容；
* 当前清除实现具备可重复执行的明确边界。

## 十五、输出格式

### 1. 总体验收结论

只能选择：

* 通过，可以进入 Step 1～Step 9 业务填充；
* 部分通过，只允许填充明确通过的步骤；
* 不通过，必须先修正业务清除；
* 证据不足，暂不能进入业务填充。

### 2. 九步真实职责与调用链

| Step | 真实职责 | 入口 | 公共逻辑 | Journey 业务 | 输出 |
| ---- | ---- | -- | ---- | ---------- | -- |

### 3. 每一步业务指纹

分别列出 Step 1～Step 9 的模板业务内容。

### 4. 业务残留检查

| Step | 文件/位置 | 残留内容 | 分类 | 是否阻塞 |
| ---- | ----- | ---- | -- | ---- |

### 5. 公共代码保留检查

| Step | 公共能力 | 文件/函数 | 结果 | 证据 |
| ---- | ---- | ----- | -- | -- |

### 6. 步骤间依赖检查

列出九步之间的输入输出关系及当前完整性。

### 7. 填充位置检查

| Step | 填充位置 | 当前是否明确 | 是否可以直接填充 |
| ---- | ---- | ------ | -------- |

### 8. 当前清除实现评估

说明：

* 清除原理；
* 是否基于九步真实边界；
* 漏删风险；
* 误删风险；
* 是否可继续复用。

### 9. 静态和运行验证

列出实际命令、结果和未验证项。

### 10. 阻塞问题

只列阻止进入业务填充的问题。

### 11. 后续动作

明确说明：

* 是否需要修正清除实现；
* 哪些 Step 需要重新清除；
* 是否需要重新从成熟模板生成新 Journey；
* 哪些 Step 已经可以进入业务填充。

## 十六、禁止事项

* 不修改任何文件；
* 不直接修复清除实现；
* 不填充目标 Journey 业务；
* 不只分析 Step 1；
* 不假设九步使用相同清除边界；
* 不只检查 Journey 名称；
* 不把语法通过等同于业务清除成功；
* 不忽略配置、Schema、测试数据和步骤中间产物；
* 不在无法运行时声称验证通过；
* 不提出脱离现有实现的新架构。
